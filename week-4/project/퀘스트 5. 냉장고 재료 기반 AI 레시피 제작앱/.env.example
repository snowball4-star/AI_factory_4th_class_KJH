# Supabase(PostgreSQL) 연결 문자열. 퀘스트 4 앱과 같은 DB 를 쓴다.
# 실제 값은 .env 에만 두고 커밋하지 않는다.
DATABASE_URL=postgresql://<user>:<password>@<host>:6543/postgres

# /recipe 레시피 자동 생성에 쓰는 OpenAI 키. 비워 두면 데모(규칙 기반) 생성으로 동작한다.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
